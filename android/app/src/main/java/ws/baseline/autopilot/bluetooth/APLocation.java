package ws.baseline.autopilot.bluetooth;

import ws.baseline.autopilot.GeoPoint;

/**
 * Fused location and speed
 */
public class APLocation implements APEvent {
    public APLocation(GeoPoint lastPoint) {
    }
}
